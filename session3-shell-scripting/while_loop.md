# Explanantion for while_loop.sh script

We set the initial condition for the loop as true to make it infinite. Then the script prompts the user to enter a number, we first check if the user enetered q and then break the loop, if not we check if entered number is valid or not, if not we prompt the user to enter a valid number, else we simply print the number entered by the user.

